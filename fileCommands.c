#include <stdio.h>

#include "fileCommands.h"

void commands() {
    printf("\n\n");
    printf("-- COMMANDS --\n\n");
    printf("Show commands list (commands) (co)\n");
    printf("Open a file (open) (op)\n");
    printf("Read the file (read) (r)\n");
    printf("Write on the file (write) (w)\n");
    printf("Overwrite the file (overwrite) (ow)\n");
    printf("Close the file (close) (cl)\n");
    printf("Exit the file system (exit) (e)\n\n\n");
}